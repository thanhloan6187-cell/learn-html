# 🚀 HTML Learning Roadmap

## 1. Core HTML Basics
- What HTML is and how it works  
- HTML document structure  
  - `<!DOCTYPE html>`  
  - `<html>`  
  - `<head>`  
  - `<body>`  
- Elements and tags  
- Attributes (`href`, `src`, `alt`, `id`, `class`)  
- Nesting and hierarchy  

---

## 2. Essential HTML Elements

### Text & Content
- Headings: `<h1>`–`<h6>`
- Paragraphs: `<p>`
- Bold/italic: `<b>`, `<strong>`, `<i>`, `<em>`
- Lists: `<ul>`, `<ol>`, `<li>`
- Line breaks: `<br>`

### Links & Media
- Links: `<a>`
- Images: `<img>`
- Video: `<video>`
- Audio: `<audio>`

### Containers
- `<div>` (block)
- `<span>` (inline)
- Semantic tags:
  - `<header>`
  - `<nav>`
  - `<main>`
  - `<section>`
  - `<article>`
  - `<footer>`

---

## 3. Forms
- `<form>`
- `<input>` (text, email, number, password, checkbox, radio)
- `<textarea>`
- `<select>` + `<option>`
- `<button>`
- Form attributes:
  - `action`
  - `method`

---

## 4. Tables
- `<table>`
- `<tr>`, `<td>`, `<th>`
- `<thead>`, `<tbody>`, `<tfoot>`

---

## 5. HTML5 Modern Features
- `<canvas>`
- `<svg>`
- `<details>` + `<summary>`
- `<dialog>`
- Media attributes (`autoplay`, `controls`, `loop`)

---

## 6. Accessibility Basics
- `alt` text for images
- ARIA roles
- Semantic structure
- Keyboard-friendly elements

---

## 7. Best Practices
- Clean, readable code
- Proper indentation
- Use semantic tags instead of unnecessary `<div>`
- Meaningful class and id names

---

## 8. How HTML Fits With Other Web Tech
- **CSS** for styling  
- **JavaScript** for interaction  
- **DOM** for structure and manipulation
